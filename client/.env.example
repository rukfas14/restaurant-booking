# Where the dashboard calls. Set this in Vercel → Project → Settings → Environment Variables.
# Without it, the build falls back to the Railway URL hard-coded in src/lib/api.js.
VITE_API_URL=https://restaurant-booking-production-e5ce.up.railway.app
